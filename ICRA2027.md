# ICRA 2027 Task1–6 evaluation code

Published from the current local evaluation code on 2026-09-06. Commit author: ql3c20 <2991357552@qq.com>. Runtime semantics are preserved; GR00T Python formatting follows its pre-commit hooks.

This commit adds code, tests and deployment documentation only. It does not upload recordings, training datasets, videos, checkpoints, ONNX policy weights, TensorRT engines, Python environments or HSSD backgrounds. Existing upstream repository history is retained.

The integration root is https://github.com/ql3c20/Psi0_kimodo_textop/tree/icra2027 . SIMPLE is pinned by its gitlink. GR00T and the minimal HumanoidVLA_MJ resource repository revisions are pinned in `scripts/deploy/icra2027_release.json` in the integration repository. The latter repository is private and requires ql3c20-authorized access.

External inputs are still required: GR00T checkpoint and Cosmos backbone; SONIC decoder; WBC Balance/Walk weights; Task1–6 CSV/model_snapshot recordings; HSSD scene dependencies and lighting manifests; compatible Isaac Sim and Python environments. No GPU closed-loop evaluation is asserted by this source publication.

Validation details and known pre-existing Task1 test mismatches are recorded in the integration repository release notes.
